/// The parameters that the chat_screen expects to find if it has been routed to.
/// 
/// To refactor parameter names, change them only here.
class ChatScreenParameters {
  static String chatId = 'chatId';
}
